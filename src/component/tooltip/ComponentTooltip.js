import React from "react";
import Tooltip from "./Tooltip";

const ComponentTooltip = () => {
  return (
    <div className="w-full h-full">
      <Tooltip text="Hover me"></Tooltip>
    </div>
  );
};

export default ComponentTooltip;
